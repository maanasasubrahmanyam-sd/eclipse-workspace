package com.home.test.map;

public class Adobee2 {

	public static void main(String[] args) {

		//adding two numbers 
		// edge case one number can be negative or both can be negative
		// number is big number 
		// null check 
		// large input number 
		// number can be null
		// what if use enter text in place of int 
		// JVM don't indicate overflow during operation during int datatype.
		// overflows - addition of two large -ve
		// adding of two large +ve
		// addition of two large opposti sign
		
		
		
		
	}
	
	/*
	 * public static boolean scrollDown (int a){ return (a == 0) ? scrollDown(100) :
	 * scrollDown(a-1); }
	 * 
	 * public static void main(int arg1, int arg2) { if (arg2 <= 0) if (arg2 < 0)
	 * System.out.println("Too small."); else System.out.println("Just right.");
	 * scrollDown(arg1); }
	 */

}

/*
 * Program will result in infinite loop, as scrollDown method is recursive and
 * variable a’s value is not decrementing.
 * 
 * Other problems are 1) Program won't compile as it has compiler error. 2)
 * Missing semicolon. 3) Bad signature main(int arg1, arg2). Missing return type
 * and parameter declaration. 4) scrollDown(arg1, "start"); wrong parameters
 * declaration, method is expecting only one argument. 5) public static
 * scrollDown (Integer a) missing return type 6) Non static method is calling
 * static method. 7) (a = 0) wrong declaration of comparison operator print
 * syntax is wrong.
 */

 

